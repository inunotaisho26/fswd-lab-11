# Full Stack Web Development -  Lab 11: Angular Components

## Setup

* Fork and clone, as always
* `npm install`
* `npm run dev` (**not** `npm start`)

## Tasks

* Transition app to using Angular Components
